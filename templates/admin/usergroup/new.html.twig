{% extends 'base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 mt-5">
    <h4>Add a new usergroup for {{ car.name }}</h4>

    {{ form_start(usergroupForm)}}
        {{ form_widget(usergroupForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">Add Usergroup</button>
    {{ form_end(usergroupForm) }}
</div>
{% endblock %}