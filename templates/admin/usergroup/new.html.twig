{% extends 'base.html.twig' %}

{% block content_body %}
    <h1>Add a new usergroup for {{ car.name }}</h1>

    {{ form_start(usergroupForm)}}
        {{ form_widget(usergroupForm) }}

        <button type="submit" class="btn btn-primary">Add Usergroup</button>
    {{ form_end(usergroupForm) }}
{% endblock %}