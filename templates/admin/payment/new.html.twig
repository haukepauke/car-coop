{% extends 'base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 col-md-4 mt-5">
    <h4>{{ 'payments.add.for'|trans }} {{ car.name }}</h4>

    {{ form_start(paymentForm)}}
        {{ form_widget(paymentForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">{{ 'payments.add.add'|trans }}</button>
    {{ form_end(paymentForm) }}
</div>
{% endblock %}