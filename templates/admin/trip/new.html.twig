{% extends 'admin/base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 col-md-4">
    <h4>Add a new trip with {{ car.name }}</h4>

    {{ form_start(tripForm)}}
        {{ form_widget(tripForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">Add trip</button>
    {{ form_end(tripForm) }}
</div>
{% endblock %}