{% extends 'base.html.twig' %}

{% block content_body %}
    <h1>Edit expense with {{ car.name }}</h1>

    {{ form_start(expenseForm)}}
        {{ form_widget(expenseForm) }}

        <button type="submit" class="btn btn-primary">Edit expense</button>
    {{ form_end(expenseForm) }}
{% endblock %}