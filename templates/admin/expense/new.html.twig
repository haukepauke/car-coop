{% extends 'admin/base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 col-md-4">
    <h4>Add a new expense with {{ car.name }}</h4>

    {{ form_start(expenseForm)}}
        {{ form_widget(expenseForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">Add expense</button>
    {{ form_end(expenseForm) }}
</div>
{% endblock %}