{% extends 'base.html.twig' %}

{% block content_body %}
    <h1>Update {{ car.name }}</h1>

    {{ form_start(carForm)}}
        {{ form_widget(carForm) }}

        <button type="submit" class="btn btn-primary">Edit car</button>
    {{ form_end(carForm) }}
{% endblock %}