{% extends 'base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 mt-5">
    <h4>{{ 'booking.edit.for'|trans }}</h4>

    {% if overlappingBookings is not empty %}
    <div class="alert alert-warning">
        <p class="mb-2">{{ 'booking.overlap.inline'|trans({'%count%': overlappingBookings|length}) }}</p>
        <ul class="mb-0">
            {% for overlap in overlappingBookings %}
            <li>
                {{ overlap.startDate|date('Y-m-d H:i') }} – {{ overlap.endDate|date('Y-m-d H:i') }}
                · {{ overlap.user.name }}
                {% if overlap.title %}
                    · {{ overlap.title }}
                {% endif %}
            </li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    {{ form_start(bookingForm) }}
        {{ form_widget(bookingForm) }}
    {{ form_end(bookingForm, {'render_rest': false}) }}

    <div class="d-flex flex-wrap gap-2 align-items-center mt-3">
        <button type="submit" form="{{ bookingForm.vars.id }}" class="btn btn-outline-dark">{{ 'booking.update'|trans }}</button>
        <form method="post" action="{{ path('app_booking_delete',  {booking: booking.id}) }}" class="d-inline">
            <input type="hidden" name="_token" value="{{ csrf_token('booking_delete_' ~ booking.id) }}">
            <button type="submit" class="btn btn-outline-danger">{{ 'booking.delete'|trans }} <i class="fas fa-trash"></i></button>
        </form>
    </div>
</div>
{% endblock %}
