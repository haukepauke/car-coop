{% extends 'base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 col-md-4 mt-5">
    <h4>{{ 'booking.add.for'|trans }} {{ car.name }}</h4>

    {{ form_start(bookingForm)}}
        {{ form_widget(bookingForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">{{ 'booking.create'|trans }}</button>
    {{ form_end(bookingForm) }}
</div>
{% endblock %}