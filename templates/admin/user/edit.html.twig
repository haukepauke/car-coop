{% extends 'admin/base.html.twig' %}

{% block content_body %}
<div class="col-xl-6 col-md-4">
    <h4>Edit profile</h4>

    {{ form_start(userForm)}}
        {{ form_widget(userForm) }}

        <button type="submit" class="btn btn-outline-dark my-3">Update your profile</button>
    {{ form_end(userForm) }}
</div>
{% endblock %}